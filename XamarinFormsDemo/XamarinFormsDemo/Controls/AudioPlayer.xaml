<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFormsDemo.Controls.AudioPlayer">
    <ContentView.Resources>

    </ContentView.Resources>
    <ContentView.Content>

        <StackLayout>
            <Label Text="{Binding Message}" HorizontalTextAlignment = "Center" TextColor="White"/>

            <Image Source="{Binding SongImage}" Margin="8"/>

            <Grid>
                <StackLayout Margin="16" HorizontalOptions="Center" VerticalOptions="Center">
                    <Image Source="{Binding TransportImage}" WidthRequest="25" HeightRequest="25"/>
                </StackLayout>

                <Button 
                    x:Name="Transport"           
                    Clicked="Button_OnClicked" 
                    WidthRequest="100"
                    HeightRequest="100" 
                    CornerRadius="50"
                    HorizontalOptions="Center" 
                    BackgroundColor="Transparent"
                    BorderColor="White"
                    BorderWidth="2"
                />
            </Grid>
            <ProgressBar Progress="{Binding Progress}" ProgressColor="White"/>

            <ActivityIndicator Color="White" IsRunning="{Binding IsLoading}"/>
      </StackLayout>
    </ContentView.Content>
</ContentView>